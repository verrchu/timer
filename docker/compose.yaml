services:
  timer-postgres:
    container_name: timer-postgres
    build:
      context: ./images/postgres
    networks:
      - timer
    ports:
      - "5555:5432"

networks:
  timer:
    driver: bridge
